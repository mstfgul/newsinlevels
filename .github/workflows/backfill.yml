name: Backfill language

on:
  workflow_dispatch:
    inputs:
      lang:
        description: "Language code to backfill (en/de/fr/es)"
        default: "es"

permissions:
  contents: write

jobs:
  backfill:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Backfill language versions
        run: node pipeline/backfill-language.mjs ${{ github.event.inputs.lang || 'es' }}
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Commit updated articles
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "Nothing to backfill."
          else
            git commit -m "news: backfill ${{ github.event.inputs.lang || 'es' }} versions"
            # The branch may have moved while we were generating — rebase, then push.
            git pull --rebase origin main
            git push
          fi
